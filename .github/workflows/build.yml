name: Build B1C1 Android 10 Kirisakura Kernel + AnyKernel3

on:
  workflow_dispatch:
  push:
    branches: [kirisakura]

permissions:
  contents: write

env:
  ARCH: arm64
  DEFCONFIG: b1c1_defconfig
  AK3_REPO: https://github.com/osm0sis/AnyKernel3.git

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout source
        uses: actions/checkout@v4
        with:
          ref: kirisakura
          fetch-depth: 1
          submodules: false

      - name: Copy kernel source to runner temp
        shell: bash
        run: |
          set -euxo pipefail
          rm -rf "$RUNNER_TEMP/b1c1-kernel"
          mkdir -p "$RUNNER_TEMP/b1c1-kernel"
          cp -a ./. "$RUNNER_TEMP/b1c1-kernel/"
          echo "KERNEL_WORKDIR=$RUNNER_TEMP/b1c1-kernel" >> "$GITHUB_ENV"
          test -f "$RUNNER_TEMP/b1c1-kernel/Makefile"

      - name: Install dependencies
        shell: bash
        run: |
          set -euxo pipefail
          sudo apt-get update
          sudo apt-get install -y python3 device-tree-compiler git build-essential gcc g++ zip unzip lz4

      - name: Install Android 10 mkdtimg
        shell: bash
        run: |
          set -euxo pipefail
          git clone --depth=1 --branch android-10.0.0_r47 https://android.googlesource.com/platform/prebuilts/misc "$RUNNER_TEMP/prebuilts-misc"
          MKDTIMG="$RUNNER_TEMP/prebuilts-misc/linux-x86/libufdt/mkdtimg"
          test -f "$MKDTIMG"
          test -x "$MKDTIMG"
          sudo install -m 0755 "$MKDTIMG" /usr/local/bin/mkdtimg
          mkdtimg help all

      - name: Prepare B1C1 A10 baseline
        shell: bash
        working-directory: ${{ env.KERNEL_WORKDIR }}
        run: |
          set -euxo pipefail
          chmod +x scripts/prepare_b1c1_a10.sh
          scripts/prepare_b1c1_a10.sh

      - name: Verify B1C1 source
        shell: bash
        working-directory: ${{ env.KERNEL_WORKDIR }}
        run: |
          set -euxo pipefail
          test -f Makefile
          test -f "arch/${ARCH}/configs/${DEFCONFIG}"
          grep -q '^CONFIG_BOARD_B1C1=y$' "arch/${ARCH}/configs/${DEFCONFIG}"
          grep -q '^# CONFIG_LTO_CLANG is not set$' "arch/${ARCH}/configs/${DEFCONFIG}"
          grep -q '^CONFIG_CFI_CLANG=y$' "arch/${ARCH}/configs/${DEFCONFIG}"
          grep -q '^CONFIG_SHADOW_CALL_STACK=y$' "arch/${ARCH}/configs/${DEFCONFIG}"

      - name: Build kernel with AnyKernel3
        uses: dabao1955/kernel_build_action@v1.10.2
        with:
          kernel-url: ${{ env.KERNEL_WORKDIR }}
          kernel-branch: kirisakura
          config: b1c1_defconfig
          arch: arm64
          android-version: 10
          aosp-clang: true
          aosp-clang-version: r353983c
          aosp-gcc: true
          disable-lto: true
          anykernel3: true
          anykernel3-url: ${{ env.AK3_REPO }}
          release: false
          extra-make-args: '["KCFLAGS=-Wno-missing-braces"]'

      - name: Locate kernel and DTBO outputs
        shell: bash
        run: |
          set -euxo pipefail
          KERNEL="$(find "$GITHUB_WORKSPACE" "$RUNNER_TEMP" -type f -name 'Image.lz4-dtb' -print -quit)"
          DTBO="$(find "$GITHUB_WORKSPACE" "$RUNNER_TEMP" -type f -name 'dtbo.img' -print -quit)"
          test -n "$KERNEL"
          test -s "$KERNEL"
          test -n "$DTBO"
          test -s "$DTBO"
          echo "KERNEL_IMAGE=$KERNEL" >> "$GITHUB_ENV"
          echo "DTBO_IMAGE=$DTBO" >> "$GITHUB_ENV"

      - name: Build flash-ready Pixel 3 AnyKernel3 from Osmosis template
        shell: bash
        run: |
          set -euxo pipefail
          rm -rf "$RUNNER_TEMP/anykernel-package"
          git clone --depth=1 "$AK3_REPO" "$RUNNER_TEMP/anykernel-package"
          cd "$RUNNER_TEMP/anykernel-package"
          rm -rf .git .github
          rm -f README.md Image Image.* zImage zImage.* dtb dtb.img dtbo.img anykernel.sh
          cp "$KERNEL_IMAGE" ./Image.lz4-dtb
          cp "$DTBO_IMAGE" ./dtbo.img
          {
            echo '# AnyKernel3 - Bias8145 B1C1 Pixel 3 Android 10'
            echo 'properties() {'
            echo "  kernel.string=Bias8145 B1C1 Android 10 Kirisakura Kernel"
            echo '  do.devicecheck=1'
            echo '  do.modules=0'
            echo '  do.systemless=0'
            echo '  do.cleanup=1'
            echo '  do.cleanuponabort=0'
            echo '  device.name1=blueline'
            echo '  supported.versions=10'
            echo '  supported.patchlevels='
            echo '  supported.vendorpatchlevels='
            echo '}'
            echo 'BLOCK=boot;'
            echo 'IS_SLOT_DEVICE=1;'
            echo 'RAMDISK_COMPRESSION=auto;'
            echo 'PATCH_VBMETA_FLAG=auto;'
            echo '. tools/ak3-core.sh;'
            echo 'dump_boot;'
            echo 'flash_dtbo;'
            echo 'write_boot;'
          } > anykernel.sh
          chmod 0755 anykernel.sh
          grep -q 'device.name1=blueline' anykernel.sh
          grep -q 'supported.versions=10' anykernel.sh
          grep -q '^BLOCK=boot;' anykernel.sh
          grep -q '^IS_SLOT_DEVICE=1;' anykernel.sh
          grep -q 'flash_dtbo;' anykernel.sh
          grep -q 'write_boot;' anykernel.sh
          test -s Image.lz4-dtb
          test -s dtbo.img
          cd "$GITHUB_WORKSPACE"
          rm -f B1C1-A10-Kirisakura-AnyKernel3.zip B1C1-A10-Kirisakura-AnyKernel3.sha256
          cd "$RUNNER_TEMP/anykernel-package"
          zip -r9 "$GITHUB_WORKSPACE/B1C1-A10-Kirisakura-AnyKernel3.zip" . -x '.git/*' '.github/*' 'README.md' '*placeholder*'
          cd "$GITHUB_WORKSPACE"
          unzip -t B1C1-A10-Kirisakura-AnyKernel3.zip
          unzip -l B1C1-A10-Kirisakura-AnyKernel3.zip | grep -E 'Image\.lz4-dtb|dtbo\.img|anykernel\.sh|tools/ak3-core\.sh|META-INF/com/google/android/update-binary|LICENSE'
          sha256sum B1C1-A10-Kirisakura-AnyKernel3.zip | tee B1C1-A10-Kirisakura-AnyKernel3.sha256

      - name: Upload flash-ready AnyKernel3
        uses: actions/upload-artifact@v4
        with:
          name: B1C1-A10-Kirisakura-AnyKernel3-ready
          path: |
            B1C1-A10-Kirisakura-AnyKernel3.zip
            B1C1-A10-Kirisakura-AnyKernel3.sha256

      - name: Create GitHub pre-release
        if: github.event_name == 'workflow_dispatch'
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euxo pipefail
          TAG="b1c1-a10-kirisakura-${GITHUB_RUN_NUMBER}"
          gh release create "$TAG" B1C1-A10-Kirisakura-AnyKernel3.zip B1C1-A10-Kirisakura-AnyKernel3.sha256 --repo "$GITHUB_REPOSITORY" --prerelease --title "B1C1 Android 10 Kirisakura Kernel ${GITHUB_RUN_NUMBER}" --notes "Pixel 3 (blueline) Android 10 test kernel. Built from branch kirisakura using the proven test-branch AnyKernel3 packaging flow, with Osmosis AnyKernel3 as the packaging template. Boot compatibility must be hardware validated before normal use."
